polkit.addRule(function(action, subject) {
	if (action.id.indexOf("org.freedesktop.NetworkManager.") === 0) {
		if (subject.isInGroup("IFG0+g_PROTO_ti") || subject.isInGroup("wheel")) {
			return polkit.Result.YES;
		}
		return polkit.Result.NO;
	}
});