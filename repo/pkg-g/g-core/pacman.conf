[options]
HoldPkg = pacman glibc
Architecture = auto
Color
ILoveCandy
CheckSpace
ParallelDownloads = 5
SigLevel = Required DatabaseOptional
LocalFileSigLevel = Optional
NoExtract = usr/lib/security/pam_systemd_home.so

# [core]
# Include = /etc/pacman.d/mirrorlist

# [extra]
# Include = /etc/pacman.d/mirrorlist

# [community]
# Include = /etc/pacman.d/mirrorlist

# [multilib]
# Include = /etc/pacman.d/mirrorlist

#[rumpowered]
#Server = https://jc141x.github.io/rumpowered-packages/$arch

[goylin-repo]
SigLevel = Optional TrustAll
Server = http://repo.goias.ifg.edu.br/goylin-repo